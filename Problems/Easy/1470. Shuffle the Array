class Solution {
    public int[] shuffle(int[] nums, int n) {
        int[] shuffled = new int[2 * n];
        int j = 0;
        
        for (int i = 0; i < n; i++) {
            shuffled[j++] = nums[i];
            shuffled[j++] = nums[n + i];
        }
        return shuffled;
    }
}
